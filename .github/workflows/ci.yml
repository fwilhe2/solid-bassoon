name: CI
on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: |
        sleep 20
    - name: Test
      run: |
        sleep 20
    - name: Deploy
      run: |
        sleep 20
    - name: Smoke Test
      run: |
        sleep 20

